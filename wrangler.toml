name = "r2-proxy"  # This can be changed in Cloudflare dashboard when using Git integration
main = "index.js"
compatibility_date = "2023-07-01"

# Environment variables and service bindings are configured in the Cloudflare dashboard
# when using Git integration, so they are not specified here