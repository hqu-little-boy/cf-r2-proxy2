name = "cf-r2-proxy"
main = "src/index.js"
compatibility_date = "2023-07-01"

# Note: R2 binding will be configured in the Cloudflare dashboard
# after deployment when connecting your R2 bucket
# Do not specify the bucket name here for initial deployment